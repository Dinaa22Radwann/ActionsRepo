on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: 'Before2'
        run: ls

      - uses: Actions/checkout@v3 

       - name: 'After2'
        run: ls