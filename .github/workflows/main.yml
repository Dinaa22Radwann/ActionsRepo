on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest

    steps:

      - uses: Actions/checkout@v3 
        
      - name: 'login to docker hub'
        run: docker login --username ${{ secrets.USERNAME }} --pasword ${{ secrets.DOCKERHUB_TOKEN }} 
      